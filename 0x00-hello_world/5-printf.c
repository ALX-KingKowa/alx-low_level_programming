#include <stdio.h>

/**
 * main - prints the string to std output using printf
 *
 * Return: returns 0 if every thing is OK
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
